Implement the Forum-iOS Jira ticket I name.

Follow `~/.cursor/skills/implement-forum-ticket/SKILL.md` exactly: fetch it with
`jira get`, read the docs it routes to, present a plan, branch as
`feature/<KEY>-<kebab-summary>`, then implement.
