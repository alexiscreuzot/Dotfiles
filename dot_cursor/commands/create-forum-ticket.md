Create a Forum-iOS Jira ticket from the following input.

Follow `~/.cursor/skills/create-forum-ticket/SKILL.md` exactly: load
`~/.cursor/forum/ticket-preferences.json`, draft the payload, confirm fields
with recommendations, then create it with `jira create`.
